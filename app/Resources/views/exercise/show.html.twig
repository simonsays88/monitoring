{% extends 'base.html.twig' %}

{% block pageTitle %}CostaFitness : Liste des exercices{% endblock %}
{% block headingTitle %}Exercice : {{ exercise.name }}{% endblock %}
{% block pageClass %}exerciseList{% endblock %}

{% block body %}

    <aside>
        <a href="{{ path('exercise_index') }}" class="btn">Retour à la liste</a>
    </aside>
    <article>
            <header>{{ exercise.name }}</header>
            <main>{{ exercise.description }}</main>
            <footer>
                <a href="{{ path('exercise_edit', { 'id': exercise.id }) }}">Modifier</a>
                {{ form_start(delete_form) }}
                    <input type="submit" value="Supprimer" class="delete btn">
                {{ form_end(delete_form) }}
            </footer>
        </article>

{% endblock %}
