{% extends 'base.html.twig' %}

{% block body %}
    <h1>Exercice</h1>

    <table>
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ exercise.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ exercise.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('exercise_index') }}">Retour à la liste</a>
        </li>
        <li>
            <a href="{{ path('exercise_edit', { 'id': exercise.id }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
