{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pack edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('pack_index') }}">Liste des packs</a>
        </li>
        <li>
            <a href="{{ path('exercise_index') }}">Liste des exercices</a>
        </li>
    </ul>
{% endblock %}
